{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step for Python API'",
    "lint": "echo 'No lint step configured'",
    "typecheck": "echo 'No typecheck step for Python API'"
  }
}
